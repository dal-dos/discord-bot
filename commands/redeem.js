module.exports = {
    name: 'redeem',
    description: "This command will give command info",
    execute(message, DMembers, client, logData){
    //code
        if (message.content.includes('!redeem')) {
          if (message.content.includes('!redeem ban <!')) {
            
          }else if (message.content.includes('!redeem kick')){

          }

        }
    //code
    }
}